int add(int a, int b) { return a+b; }

float b( ) {
    int sum = add(1, 2);
    putint(sum);
    putline();
    return 1.1 + sum;
}

int c = 1;
int main( ) {
    putfloat(b());
    putline();
    putintl(c);
    return 0;
}

